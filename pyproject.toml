[project]
name = "home-framework"
version = "0.1.0"
description = "Framework déclaratif de CAO, assemblage, BOM et débit pour l'habitat"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
  "build123d>=0.10,<0.11",
  "matplotlib>=3.10,<4",
  "ocp-vscode>=2.9,<3",
  "openseespy>=3.8,<3.9",
]

[tool.uv]
package = false
